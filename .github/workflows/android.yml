name: Build Android App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4  # عدل هذا أيضًا
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4  # وهذا أيضًا
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3  # تحديث هذا
        
    - name: Build with Gradle
      run: |
        chmod +x gradlew
        ./gradlew assembleDebug
        
    - name: Upload APK
      uses: actions/upload-artifact@v4  # هذا هو التغيير الرئيسي
      with:
        name: android-app
        path: app/build/outputs/apk/debug/*.apk
